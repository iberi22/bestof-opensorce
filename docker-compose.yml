version: '3.8'

services:
  video-generator:
    build: .
    container_name: op-to-video-generator
    volumes:
      - .:/app
      - ./output:/app/output
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    command: python src/main.py --mode local
    tty: true
